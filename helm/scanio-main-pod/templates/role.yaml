apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: scanio-main-job-manager
  namespace: default
rules:
- apiGroups:
  - batch
  resources:
  - jobs
  verbs:
  - list
  - get
  - create
  - update
  - patch 
  - delete
- apiGroups: [""]
  resources: ["secrets", "pods","persistentvolumeclaims", "persistentvolumes"]
  verbs: ["get", "list", "create", "update", "patch", "delete"]